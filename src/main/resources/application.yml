jwt:
  issuer: leets.blog
  secret: ${JWT_SECRET_KEY}
spring:
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: f2da5fb562d9d7032c7d8f189505e0c4
            client-secret: JuwvLMOMai27uqp91DjHZCAPtS0N99vH
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: profile_nickname
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/blogapp?serverTimezone=Asia/Seoul
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update #개발단계에서는 update, 운영은 validate 권장
    properties:
      hibernate:
        format_sql: true
    show-sql: true  
server:
  port: 8080
